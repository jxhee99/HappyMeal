import React from 'react';
import {
  Box,
  Typography,
  Alert,
} from '@mui/material';

const UserManagement = () => {
  return (
    <Box>
      <Alert severity="info">
        사용자 관리 기능은 아직 구현되지 않았습니다. API가 준비되면 구현하겠습니다.
      </Alert>
    </Box>
  );
};

export default UserManagement; 